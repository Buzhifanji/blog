import type { Markdown } from '@vuepress/markdown';
import type { App } from '../types';
/**
 * Create markdown-it instance for vuepress app
 */
export declare const createAppMarkdown: (app: App) => Promise<Markdown>;
