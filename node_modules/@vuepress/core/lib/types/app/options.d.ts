import type { MarkdownOptions } from '@vuepress/markdown';
import type { SiteData } from '@vuepress/shared';
import type { BundlerConfig } from '../bundler';
import type { PluginConfig } from '../plugin';
import type { ThemeConfig } from '../theme';
/**
 * Vuepress app options
 */
export interface AppOptions<T extends ThemeConfig = ThemeConfig, U extends BundlerConfig = BundlerConfig> extends SiteData {
    theme: string;
    themeConfig: Partial<T>;
    bundler: string;
    bundlerConfig: Partial<U>;
    source: string;
    dest: string;
    temp: string;
    cache: string;
    public: string;
    debug: boolean;
    markdown: MarkdownOptions;
    pagePatterns: string[];
    plugins: PluginConfig[];
    host: string;
    port: number;
    open: boolean;
    templateDev: string;
    shouldPreload: ((file: string, type: string) => boolean) | boolean;
    shouldPrefetch: ((file: string, type: string) => boolean) | boolean;
    templateSSR: string;
}
export declare type AppConfig<T extends ThemeConfig = ThemeConfig, U extends BundlerConfig = BundlerConfig> = Partial<AppOptions<T, U>> & Pick<AppOptions<T, U>, 'source'>;
